<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tomho.specforu">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainPageActivity"
            android:label="@string/title_activity_main_page" />
        <activity android:name=".ShowShopActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".myshopactivity.ManageActivity" />
        <activity android:name=".myshopactivity.UploadActivity" />
        <activity android:name=".myshopactivity.AnalyseActivity" />
        <activity android:name=".myshopactivity.BillingActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".internaldatahandler.ReadFileActivity"
            android:theme="@android:style/Theme.NoDisplay" />
        <activity android:name=".shopregisterflow.SignUpShopDetailActivity" />
        <activity android:name=".shopregisterflow.SignUpBankDetailActivity" />
        <activity android:name=".shopregisterflow.ShowFinalSignUpInfoActivity"></activity>
    </application>

</manifest>